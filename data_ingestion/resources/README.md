This folder contains Databricks asset bundle resources for the data_ingestion project.

- data_ingestion_pipeline.pipeline.yml: Pipeline definition using the project wheel.
- data_ingestion_job.job.yml: Job definition for scheduled pipeline runs.
- variables.yaml: Environment variables for the bundle.
